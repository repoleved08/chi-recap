version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: chi-recap-mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_ROOT_USER: root
      MYSQL_DATABASE: chi_recap
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

volumes:
  mysql_data:
